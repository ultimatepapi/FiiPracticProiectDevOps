---
- name: Deploy Playbook
  hosts : all
  remote_user: root
  vars:
    user: "{{ lookup('env', 'CI_REGISTRY_USER') }}"
    password: "{{ lookup('env', 'CI_REGISTRY_PASSWORD') }}"
    registry_url: "{{ lookup('env', 'CI_REGISTRY') }}"
    image: "{{ lookup('env', 'CI_REGISTRY_IMAGE') }}"
    tag: "{{ lookup('env', 'CI_COMMIT_SHORT_SHA') }}"
    container_name: "springboot"

  tasks:
    - name: Log into Gitlab Container Registry
      docker_login:
        registry: "{{ registry_url }}"
        username: "{{ user }}"
        password: "{{ password }}"
      environment:
        REQUESTS_CA_BUNDLE: "/etc/pki/ca-trust/source/anchors/ca.crt"

    - name: Pull Docker image
      docker_image:
        name: "{{ image }}"
        tag: "{{ tag }}"
        source: pull

    - name: Stop and remove existing container
      docker_container:
        name: "{{ container_name }}"
        state: absent
        force_kill: yes
      ignore_errors: yes

    - name: Deploy Docker container
      docker_container:
        name: "{{ container_name }}"
        image: "{{ image }}:{{ tag }}"
        state: started

    - name: Logout from Gitlab Container Registry
      docker_login:
        state: absent

